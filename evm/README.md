# Contract

## Install Hardhat
`npm install`

## Compile with Hardhat
`npx hardhat compile`

## Test with Hardhat
`npx hardhat test` or `npx hardhat testindexit`

## Deploy with Hardhat
`npx hardhat run src/index.js`
